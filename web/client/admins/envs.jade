template(name="envTypesTable")
  .col-md-6.column
    +reactiveTable settings=settings
    hr     
    br
    
    //- +quickForm id="envTypeCreator" schema="EnvTypesSchema" type="method" meteormethod="createEnvType" 
    +testAutoForm data=testEnvTypeData autosave=true

template(name="envTypePortsField")
  ul
    each configs.servicePorts
      li {{port}} ({{type}})

template(name="testAutoForm")
  hr
  +autoForm id=formId doc=data schema="EnvTypesSchema" type="method" meteormethod="createEnvType" 
    +afQuickField name="configs.servicePorts"
  
  hr
  +autoForm id=formId doc=data schema="EnvTypesSchema" type="method" meteormethod="createEnvType"  
    +afQuickField name="configs.envs.$"




template(name="envTypeEnvsField")
  ul
    each configs.envs
      if mustHave
        li {{name}} (MUST HAVE)
      else
        li {{name}}
